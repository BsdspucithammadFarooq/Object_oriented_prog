with open (file="data.txt",mode="a") as file:
    # file.seek(6)
    # file.tell()
    # print(file.readline())
    # file.truncate(2)
    # file.flush() it will flush the buffer ,and done automatically in python when we close a file
    